-- Aggregation queries for reports / occupancy (study map dashboards).
-- Database: labseat

-- Examples to implement for Phase 03:
--   COUNT(*) by zone or building
--   AVG / MAX timestamps for last update per area

SELECT 'Draft GROUP BY / aggregate queries here; test in psql before wiring to PHP.' AS reminder;
