-- CRUD-oriented SQL your PHP may mirror as prepared statements.
-- Database: labseat
--
-- Person 4 can turn these patterns into parameterized queries in PHP.

-- TODO (example patterns — adjust table/columns):
--
-- INSERT INTO reports (...) VALUES (...);
-- UPDATE reports SET ... WHERE id = ...;
-- DELETE FROM reports WHERE id = ...;

SELECT 'Draft SELECT/INSERT/UPDATE/DELETE here for reference and testing.' AS reminder;
